> [!CAUTION]
> **必須**: 実行前に同フォルダの `SPEC.md` を読むこと（技術契約・data属性一覧）

> [!IMPORTANT]
> **コード例（JSセレクタ等）はこのファイルに書かない。**
> 実装の正本は `scripts/codexapp_cdp_client.py`、技術契約は `SPEC.md`。

# CODEXAPP Agent v1.4.0

**用途**: CODEXデスクトップアプリ（Electron）にCDP経由でテキストを送信し、応答を取得する。

---

## 📋 Protocol: 5-Phase Flow

```
┌────────────────────────────────────────────────────┐
│  1. LAUNCH 🚀                                      │
│     → CODEXAPP をポート9224でCDP付き起動            │
│     → netstat で LISTENING 確認                     │
│     ↓                                              │
│  2. CONNECT 🔌                                     │
│     → codexapp_cdp_client.py が自動接続            │
│     ↓                                              │
│  3. SEND 📝                                        │
│     → codexapp_cdp_client.py send でメッセージ送信  │
│     ↓                                              │
│  4. WAIT ⏳                                        │
│     → トークンベースのポーリングで応答完了待機      │
│     ↓                                              │
│  5. COLLECT 📥                                     │
│     → 応答テキストを取得・ファイル保存              │
└────────────────────────────────────────────────────┘
```

---

## Phase 1: LAUNCH 🚀

// turbo
```powershell
# CDPポート確認（既に起動済みならスキップ）
netstat -ano | findstr ":9224" | findstr "LISTENING"
```

起動していない場合:

```powershell
# CODEXAPP をCDP付きで起動
powershell -NoProfile -ExecutionPolicy Bypass `
  -File "Codex-Windows\scripts\run.ps1" `
  -Reuse -CdpPort 9224
```

> [!WARNING]
> 起動は `-Wait` でブロックされるためバックグラウンドで実行。
> LISTENINGになるまで最大10秒待機。

---

## Phase 2-5: SEND → WAIT → COLLECT（統合コマンド）

**基本パターン**（送信＋応答待機＋保存を一括実行）:

```powershell
# メッセージ送信＆応答取得（トークン自動付与）
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py send "質問テキスト"

# 新スレッドを開いてから送信
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py send --new-thread "質問テキスト"

# 入力欄をクリアしてから送信
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py send --clear-input "質問テキスト"

# タイムアウト指定（長時間応答用）
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py send --timeout 600 "質問テキスト"

# 応答をファイルに保存
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py -o _outputs\codexapp\response.txt send "質問テキスト"
```

**応答のみ取得**（送信済みの場合）:

```powershell
# 最新応答を取得
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py get-latest

# トークン指定で特定の応答を取得
python .agent\workflows\codex\sub_agents\app\scripts\codexapp_cdp_client.py get-latest --token "[[REQ:20260210-123456-a1b2c3d4]]"
```

**応答完了判定**（4条件AND — 詳細は `SPEC.md` 参照）:
1. 応答テキストが存在
2. 送信ボタンが活性化
3. テキストハッシュが6回連続不変
4. 「思考中」テキストでない

---

## 🔧 トラブルシューティング

| 問題 | 解決策 |
|:-----|:-------|
| ポート9224が開かない | 前インスタンスのTIME_WAIT待ち → 数秒後に再起動 |
| WebSocket拒否 | `run.ps1` に `--remote-allow-origins=*` が含まれているか確認 |
| テキストが入らない | 技術詳細は `SPEC.md` Step 2-3 参照 |
| 送信ボタンが見つからない | Enter キーフォールバック有り。技術詳細は `SPEC.md` Step 4 参照 |
| 応答が来ない | CODEXAPPのモデル設定・API Key確認。`--timeout` を延長 |

---

## 💡 Rules

- **ミス時はまずSPEC.mdを確認**
- **SPEC.md を必ず先に読む**
- **コード例（JSセレクタ等）はこのGUIDE.mdに書かない**
- **実装の正本は `codexapp_cdp_client.py`**
- **`Page.navigate`禁止**（Electron破壊）
- **ポート9224 固定**
- **Language**: 日本語
