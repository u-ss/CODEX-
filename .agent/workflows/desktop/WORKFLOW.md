---
name: Desktop Control v5.2.2
description: 自律PC操作エージェント - リアルタイム状態監視・複数確認手段（v5.2.2）
---

> [!CAUTION]
> **必須**: 実行前に同フォルダの `SKILL.md` を読むこと（ルール・技術詳細）

# Desktop Control v5.2.2 (`/desktop`)

**自律PC操作エージェント**: CDP経由でブラウザ操作、Pywinautoでウィンドウ操作。

> [!CAUTION]
> **強制ルール**:
> 1. **同フォルダのSKILL.mdを最初に読む** - 例外なし
> 2. **browser_subagent禁止** - BOT判定される
> 3. **レイヤー優先順序・禁止事項はSKILL.md参照** - 重複記載なし

## 📋 実行フロー

```
0. SKILL.md読む（必須）
   ↓
1. Perceive（観測）
   → モニターごとSS撮影
   ↓
2. Act（実行）
   → Layer 2+(CDP) → Layer 3(UIA) → Layer 1(PyAutoGUI)
   ↓
3. Verify（確認）
   → 複数確認手段で結果検証
```

## 🤖 サブコマンド

### `/desktop-chatgpt`

**ブラウザのChatGPTに相談**するときに使用。

```
/desktop-chatgpt [質問内容]
例: /desktop-chatgpt この設計についてどう思う？
```

**実行フロー**:
1. CDPでChatGPTページに接続
2. 質問送信 → **送信確認**
3. **稼働確認**（生成中検知）
4. **返答取得確認**（ハッシュ安定判定）
5. 回答取得・蓄積

---

### v5.2.2 3点検証（SKILL.md詳細参照）

| 検証 | 確認手段 | 判定基準 |
|:-----|:---------|:---------|
| **送信確認** | msg数増加・URL変化・textarea空 | 2/3以上で合格 |
| **稼働確認** | stop-button・テキスト長変化 | いずれかで合格 |
| **返答取得確認** | ハッシュ安定・エラーチェック | 2秒安定で合格 |

---

> [!CAUTION]
> **必須**: 実行前に同フォルダの `SKILL.md` を読むこと（ルール・技術詳細）

### 実行モード

> [!CAUTION]
> **singleモードを使用すること**。loopモードは非推奨。

| モード | 用途 | コマンド |
|:-------|:-----|:---------|
| `single` | **必須** Antigravity委譲 | `--mode single --question "質問"` |
| `loop` | ⚠️ 非推奨 | 使用禁止 |

```powershell
# 単発相談（必須）
python .agent\workflows\desktop\scripts\goal_driven_consultation.py \
    --mode single --question "質問内容"
```

---

## 💡 Rules

- **SKILL.md必読**
- **browser_subagent絶対禁止**
- **CDP最優先**
- **Language**: 日本語で報告
