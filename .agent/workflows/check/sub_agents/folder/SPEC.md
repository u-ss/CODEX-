# Folder Check SKILL v1.1.0
**目的**: `/check` (folder モード) 起動時にワークスペース全体を自動解析し、フォルダ構造・エージェント構造・コードベースの全体像を把握して、その情報を活用可能にする。

## 役割境界

- この SPEC.md は技術仕様（入出力・判定基準・実装詳細）の正本。
- 実行手順は同フォルダの GUIDE.md を参照。

> [!IMPORTANT]
> **v1.1**: Antigravityが直接実行する自動解析エージェント。対象はワークスペースルート（`antigravity/`）。

## 📦 解析対象

| 対象 | 解析内容 |
|:-----|:---------|
| **フォルダ構造** | ディレクトリツリー、ファイル数、サイズ分布 |
| **エージェント構造** | 2層構造（`.agent/workflows/` ↔ `エージェント/`）の対応関係 |
| **Pythonスクリプト (.py)** | AST解析: クラス/関数/import/行数 |
| **Markdown (.md)** | 見出し構造、リンク |
| **JSON/YAML** | キー構造、ネスト深度 |
| **設定ファイル** | plan.yaml, criteria.yaml 等の構造 |

## 🔧 自動検出機能

### エージェント構造マップ

ワークスペース内の全エージェントを検出し、対応関係を一覧化：

```
エージェント検出結果:
  /code       → .agent/workflows/code/         ↔ エージェント/実装エージェント/
  /check      → .agent/workflows/check/        ↔ エージェント/チェックエージェント/
  /research   → .agent/workflows/research/     ↔ エージェント/リサーチエージェント/
  /desktop    → .agent/workflows/desktop/      ↔ エージェント/デスクトップ操作エージェント/
  ...
```

### コードベースマップ

- エントリポイント（main関数、CLI）
- 主要モジュール・クラス
- テストファイルとの対応
- 共有ライブラリ（`lib/`, `shared/`）

## 📁 出力

- `_outputs/folder-check/latest/report.json` — 構造化データ
- `_outputs/folder-check/latest/report.md` — Markdownレポート
- `_outputs/folder-check/latest/agent_map.json` — エージェント構造マップ

## 📋 Antigravity実行手順

1. `folder_analyzer.py --workspace` でスキャン＆解析を自動実行
2. 出力レポートを読み取り、ワークスペースの全体像を把握
3. 把握した情報をコンテキストとして後続タスクに活用

## ⚠️ Safety Notes

- **完全に読み取り専用**（ファイル変更なし）
- 出力先ディレクトリのみ作成
- バイナリファイルは内容読み取りをスキップ

## ログ記録（WorkflowLogger統合）

> [!IMPORTANT]
> 実行時は必ずWorkflowLoggerで各フェーズをログ記録すること。
> 詳細: [WORKFLOW_LOGGING.md](../shared/WORKFLOW_LOGGING.md)

```python
import sys; sys.path.insert(0, '.agent/workflows/shared')
from workflow_logging_hook import logged_main, phase_scope
```

ログ保存先: `_logs/autonomy/{agent}/{YYYYMMDD}/`
